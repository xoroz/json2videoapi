## 1. Implementation
- [x] 1.1 Create `Caddyfile` to proxy localhost:8080 to localhost:8000
- [x] 1.2 Create `start.sh` to manage Caddy and Uvicorn processes
- [x] 1.3 Move `test.sh` to `tests/test_e2e.sh` and update its URL to satisfy Caddy port
- [x] 1.4 Verify the setup by running `start.sh` and `tests/test_e2e.sh`
- [x] 1.5 Create `stop.sh` to stop background processes
- [x] 1.6 Create `README.md` with project documentation
- [x] 1.7 Create `.gitignore` to exclude build artifacts and secrets
