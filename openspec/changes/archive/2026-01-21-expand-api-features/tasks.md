## 1. Implementation
- [x] 1.1 Update `src/schemas.py` to support all `zvid` visual types (IMAGE, VIDEO, GIF, SVG)
- [x] 1.2 Update `src/video_processor.py` (if needed) and ensure validation covers new types
- [x] 1.3 Create `logs/` directory and configure Python logging in `src/main.py`
- [x] 1.4 Create `test.sh` script that calls the API and verifies the output file exists
- [x] 1.5 Verify all visual types work by generating a complex test video
